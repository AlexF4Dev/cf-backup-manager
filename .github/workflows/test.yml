---
name: test

on: push

jobs:
  bats:
    name: bats
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - run: docker-compose run --rm -T app test/bats/bin/bats -r test/*.bats
